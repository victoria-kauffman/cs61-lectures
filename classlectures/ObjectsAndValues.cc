#include <cstdio>

/*
int main() {
    int x = 0;
    fprintf(stdout, "%d\n", x); // fprintf just needs a value here

    ++x; 
    // 0 = 0 + 1 is an invalid variable name... 0 is something you can't assign
    fprintf(stdout, "%d\n", x);
}  */
/*
int main() {
    int x = 0;
    fprintf(stdout, "%d %p\n", x, &x);
    // &x means you want the address of x

    ++ x;
    fprintf(stdout, "%d %p\n", x, &x);
}  */
